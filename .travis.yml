language: python

dist: trusty
sudo: required
group: deprecated-2017Q4

notifications:
    email: false

before_install:
    - pyenv global 2.7 3.5
    - pip install tox
    - pip install python-coveralls

script:
    - tox

after_success:
  - coveralls
